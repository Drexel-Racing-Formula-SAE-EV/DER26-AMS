# Generated parameter packages

`hil.build_parameters(..., 'Save', true)` writes hashed MAT and JSON pairs here
unless `HIL_OUTPUT_ROOT` or an explicit output directory redirects them.
Generated files must not replace reviewed source artifacts without validation.
